---
type: Changed
scope: popovers
---

Updated popover event listeners to use `handleEvent` with `this` as the handler, allowing for better management of event listeners and ensuring proper cleanup when the popover is destroyed.