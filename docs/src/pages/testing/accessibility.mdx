---
title: 'Testing | Accessibility | Corncob'
---

import AxeTests from '../../../../testing/accessibility/results/axe-results.json';
import DocsLayout from '../../layouts/DocsLayout.astro';
import AccessibilityResults from '../../layouts/partials/AccessibilityResults.mdx';

{/* 
  collecting dynamic headings from test results to populate the sidebar navigation. 
  This allows us to link directly to specific test results for each component.
  We can't use astro's MDX Frontmatter Layout: DocsLayout for dynamic headings, so we handle it manually.
*/}

export const dynamicHeadings = AxeTests.map((test) => ({
  slug: test.stickerSheet.replace('.html', ''),
  text: test.component,
  depth: 3
}));

<DocsLayout title="Testing | Accessibility | Corncob" dynamicHeadings={dynamicHeadings}>

## Accessibility Testing with Axe

We use [@axe-core/playwright](https://github.com/dequelabs/axe-core-npm/blob/develop/packages/playwright/README.md) to identify and address accessibility issues in our components. Automated tools still struggle with gradients and color contrast, so we use these tests as a starting point to identify potential issues. We then review the results and make necessary adjustments to ensure our components meet accessibility standards.

Accessibility is an ongoing effort, and we continuously work to improve our components based on these test results as well as user feedback. Remember, just because something is accessible in automated tests doesn't mean it's fully accessible in all real-world scenarios. 

These are automated tests that run against our sticker sheets, which are the visual representations of our components. The results are categorized into three groups: Passes, Incomplete, and Violations. Below are the detailed results for each component tested.

{
  AxeTests.map((test) => (
    <AccessibilityResults key={test.stickerSheet} test={test} />
  ))
}

</DocsLayout>

