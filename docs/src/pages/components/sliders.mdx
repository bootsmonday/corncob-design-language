---
layout: ../../layouts/DocsLayout.astro
title: 'Component | Sliders | Corncob'
---

import CodeSample from '../../layouts/partials/CodeSample.astro';
import { Code } from 'astro/components';
import sliderTokens from '../../../../src/components/sliders/slider-tokens.css?raw';

## Sliders

Sliders are used to allow users to select a value from a range. They can be used in forms, settings, and anywhere else where a range selection is needed.
Corncob sliders are standard HTML range inputs styled to fit the Corncob design system.

### Default Sliders

<div class="corn-slider">
  <label for="cornSlider">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider" />
</div>

<CodeSample id="default-slider-code">
  ```html
<div class="corn-slider">
  <label for="cornSlider">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider" />
</div>
  ```
</CodeSample>

## Size Modifiers

The default size for the slider is `md`, but it can be modified to `xs`, `sm`, `lg`, or `xl` using the corresponding class on the `corn-slider` element.

- `corn-slider--xs`
- `corn-slider--sm`
- `corn-slider--md`
- `corn-slider--lg`
- `corn-slider--xl`

### Extra Small (xs)
The `xs` size is the smallest size available for sliders. It is designed for use in compact spaces where a smaller input field is needed.
This should not be used as the default size for sliders, but it can be used in specific contexts where space is limited, such as in a table or a navigation bar.

<div class="corn-slider corn-slider--xs">
  <label for="cornSlider--xs">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--xs" />
</div>

<CodeSample id="extra-small-slider-code">
  ```html
<div class="corn-slider corn-slider--xs">
  <label for="cornSlider--xs">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--xs"  />
</div>
  ```
</CodeSample>

### Small (sm)

The `sm` size is slightly larger than `xs` and is suitable for use in situations where a smaller input field is needed, but there is more space available than for the `xs` size. It can be used in forms where space is limited, such as in a sidebar or a mobile layout.
<div class="corn-slider corn-slider--sm">
  <label for="cornSlider--sm">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--sm" />
</div>

<CodeSample id="small-slider-code">
  ```html
<div class="corn-slider corn-slider--sm">
  <label for="cornSlider--sm">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--sm" />
</div>
  ```
</CodeSample>

### Medium (md)
The `md` size is the default size for sliders. It is suitable for use in most situations and is the recommended size for general use.
<div class="corn-slider corn-slider--md">
  <label for="cornSlider--md">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--md" />
</div>

<CodeSample id="medium-slider-code">
  ```html
<div class="corn-slider">
  <label for="cornSlider--md">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--md" />
</div>
  ```
</CodeSample>

### Large (lg)
The `lg` size is larger than the default `md` size and is suitable for use in situations where a larger input field is needed, such as in a desktop layout or a settings page.
<div class="corn-slider corn-slider--lg">
  <label for="cornSlider--lg">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--lg" />
</div>

<CodeSample id="large-slider-code">
  ```html
<div class="corn-slider corn-slider--lg">
  <label for="cornSlider--lg">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--lg" />
</div>
  ```
</CodeSample>

### Extra Large (xl)
The `xl` size is the largest size available for sliders. It is designed for use in situations where an extra large input field is needed. Let us now when you find a good use for this size, as it is not commonly needed and we want to make sure it is worth including in the package.

<div class="corn-slider corn-slider--xl">
  <label for="cornSlider--xl">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--xl" />
</div>

<CodeSample id="extra-large-slider-code">
  ```html
<div class="corn-slider corn-slider--xl">
  <label for="cornSlider--xl">Adjust the Corn Slider:</label>
  <input type="range" min="0" max="100" value="50" id="cornSlider--xl" />
</div>
  ```
</CodeSample>


## Slider Tokens
<CodeSample id="slider-tokens-code">
  <Code code={sliderTokens} lang="css" />
</CodeSample>
